# Pinned environment for the EEI analysis pipeline (scripts/*).
#
# Python: 3.13.x is required. pip cannot install a specific Python patch,
#   so the exact tested interpreter (3.13.5) can only be guaranteed by
#   installing that interpreter yourself. On any other 3.13.x environment the
#   pipeline validates
#   with a warning; --strict-environment / --strict-env enforce exact pins.
# Tested environment (2026-08-16): Python 3.13.5, NumPy 2.4.1, SciPy 1.17.0.
numpy==2.4.1
scipy==1.17.0